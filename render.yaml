services:
  - type: web
    name: sainya
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn sainya_backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
    plan: free
